# Password-Policy-Enforcer-
A library/module that enforces configurable passwords policies-minimum length, complexity rule, dictionary check, breached password check via HIBP API - plug into any project.
